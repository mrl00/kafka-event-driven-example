# Example .env file for kafka-event-driven-example

# Comma-separated list of Kafka brokers
KAFKA_BROKERS=localhost:29092,localhost:39092,localhost:49092

# Kafka topic name (optional; default: orders)
KAFKA_TOPIC=orders

# Kafka consumer group ID (consumer only; default: order-consumer-group)
KAFKA_GROUP_ID=order-consumer-group

# Partition count for topic (optional; default: 3)
KAFKA_NUM_PARTITIONS=3

# Replication factor for topic (optional; default: 3)
KAFKA_REPLICATION_FACTOR=3

# HTTP port for web server (default: 4000)
HTTP_PORT=4000
